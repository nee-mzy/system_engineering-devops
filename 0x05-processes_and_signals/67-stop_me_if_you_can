#!/usr/bin/env bash
# Send SIGTERM to any running 7-highlander process

# Find PIDs of processes whose command line contains "7-highlander" and send SIGTERM
pids=$(pgrep -f "7-highlander") || pids=""
if [ -n "$pids" ]; then
  kill -TERM $pids 2>/dev/null || true
fi
